# Variables d'environnement pour Vite
# =====================================
# 
# NOTE: Les variables doivent commencer par VITE_ pour être exposées au client
# Les variables WAOS_VUE_* sont toujours supportées via generateConfig

# Port de l'application (défini aussi dans src/config/defaults/*.cjs)
# VITE_PORT=8080

# API Configuration (ou utilisez WAOS_VUE_api_*)
# VITE_API_URL=http://localhost:3000
# VITE_API_BASE=api

# Analytics
# VITE_POSTHOG_HOST=
# VITE_POSTHOG_KEY=

# Variables WAOS personnalisées (via generateConfig)
# ===================================================
# Ces variables sont mergées dans la config générée (src/config/index.cjs)
# Syntaxe: WAOS_VUE_<chemin.vers.config>=valeur
#
# Exemples:
# WAOS_VUE_port=8080
# WAOS_VUE_api.protocol=https
# WAOS_VUE_api.host=api.example.com
# WAOS_VUE_api.port=443
# WAOS_VUE_sign.route=/dashboard
# WAOS_VUE_analytics.posthog.host=ph_instance_address
# WAOS_VUE_analytics.posthog.key=ph_project_api_key
